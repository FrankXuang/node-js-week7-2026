-- ============================================================
-- 工單 6（前任值班交接）：改寫這條查詢，讓它吃到「已經存在」的索引
--
-- 原查詢（慢——函式包住欄位，索引吃不到）：
--   SELECT count(*) AS total FROM course_bookings
--   WHERE DATE(created_at) = DATE '2026-06-24';
--
-- 你的任務：把「同樣意思」的查詢用不包函式的方式寫出來
-- （查的是 2026-06-24 台北時間一整天的報名數，結果必須跟原查詢一致）
-- ⚠️ 不准建任何新索引——營運說資料庫寫入已經夠慢了
-- ============================================================

-- 把你的改寫版本寫在下面（取代這行註解）：
